import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const About = () => {
    return (
        <View>
            <Text className=' bg-black'>About</Text>
        </View>
    );
}

const styles = StyleSheet.create({})

export default About;
